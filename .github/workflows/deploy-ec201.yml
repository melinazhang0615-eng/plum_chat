script: |
  set -e

  cd /opt/workspace/plum_chat

  echo "Before:"
  git rev-parse --short HEAD

  git fetch origin
  git checkout main
  git pull --ff-only origin main

  echo "After:"
  git rev-parse --short HEAD

  git status
